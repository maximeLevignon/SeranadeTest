for (int i = 0; i < 10; ++i) {
    printf("%s\t%d\n", (ptr + i)->a, (ptr + i)->b);
  }

add for int i equals zero semicolon i less than ten semicolon plus plus i

add one word printf of quotes percent s backslash t percent d backslash n
    insert coma of p t r plus i arrow a
    insert coma of p t r plus i arrow b

main(void) {
}

for (int i = 0; i < 10; ++i) {
  printfof "%s\t%d\n";, (ptr + i)->a
}
